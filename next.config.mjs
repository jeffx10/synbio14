/** @type {import('next').NextConfig} */
const nextConfig = {
    // output: "export",
    eslint: {
        ignoreDuringBuilds: true,
      },
    // output: 'export',
    // basePath: '/temp',
    // images: {
    //     unoptimized: true,
    // }
};

export default nextConfig;
